# IP 白名单配置确认

## ✅ 当前状态

### 已完成配置
- [x] **密钥文件**：`key/private.wxd244b605ba704aab.key` ✅
- [x] **小程序构建**：构建成功 ✅
- [x] **环境变量**：`.env.local` 已配置 ✅

### ⚠️ 需要配置
- [ ] **IP 白名单**：需要添加到微信公众平台

---

## 🔍 测试结果

**错误信息**：
```
invalid ip: 115.191.1.197
```

**原因**：当前服务器 IP `115.191.1.197` 不在白名单中

---

## 📋 需要添加的 IP 地址

**服务器出口 IP**：
```
115.191.1.197
```

**其他可能的 IP**（建议一并添加）：
```
101.126.128.57
```

---

## 🔧 配置步骤

### 第一步：登录微信公众平台
```
https://mp.weixin.qq.com
```

### 第二步：进入开发设置
```
开发管理 → 开发设置 → 小程序代码上传 → IP白名单
```

### 第三步：添加 IP
点击"修改"，添加以下 IP：
```
115.191.1.197
101.126.128.57
```

### 第四步：保存配置
- 点击"确定"
- 管理员扫码确认

---

## ✅ 配置完成后

保存后立即生效，然后重新执行：

```bash
npx miniprogram-ci upload \
  --pp ./dist-weapp \
  --pkp ./key/private.wxd244b605ba704aab.key \
  --appid wxd244b605ba704aab \
  --uv 1.0.0 \
  -r 1 \
  --desc "章鱼经纪人 v1.0.0 首次发布"
```

---

## 📞 如有疑问

如果配置后仍报错：
1. 检查 IP 是否正确添加
2. 等待 1-2 分钟重试
3. 联系扣子平台确认完整 IP 列表

---

**更新时间**：2026-03-27 21:50
